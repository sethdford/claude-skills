---
description: Design and write unit tests using TDD workflow (Red-Green-Refactor).
argument-hint: [feature-description or code-block]
---

Chain these steps:

1. Use the `test-strategy` skill to identify test type (unit/integration/E2E)
2. Use the `tdd-workflow` skill to structure Red-Green-Refactor cycle
3. Use the `unit-test-design` skill to design isolated test with mocks
4. Use the `test-data-management` skill to create minimal test data
5. Generate complete test file with descriptive names and clear assertions

After completion, ask: "Would you like me to run these tests or add edge case coverage?"
