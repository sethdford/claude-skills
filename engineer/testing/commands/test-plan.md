---
description: Full integration test plan for multi-component systems.
argument-hint: [system-design or component-list]
---

Chain these steps:

1. Use the `integration-test-design` skill to identify component boundaries
2. Use the `test-strategy` skill to balance unit vs. integration testing
3. Use the `property-based-testing` skill to identify invariant properties
4. Use the `performance-test-plan` skill to set SLA targets
5. Synthesize complete test strategy with specific tests and success criteria

After completion, ask: "Should we implement load testing as part of this plan?"
