---
description: Design database schema for new feature with normalization and constraints.
argument-hint: [feature-requirements or data-model]
---

Chain these steps:

1. Use the `schema-design` skill to model entities and relationships
2. Use the `data-integrity-patterns` skill to add constraints
3. Use the `index-design` skill to plan query indexes
4. Generate schema DDL with documentation and examples

After completion, suggest: "Next, plan migrations for deploying this schema safely."
