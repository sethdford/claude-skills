---
description: System design combining multiple patterns (concurrency, state, reactive, etc.).
argument-hint: [system-requirements or design-challenge]
---

Chain these steps:

1. Use the `concurrency-patterns` skill to choose concurrency model
2. Use the `functional-patterns` skill for data transformation
3. Use the `reactive-patterns` skill if event-driven is needed
4. Use the `state-management` skill for modeling state
5. Synthesize complete design with pattern rationale and tradeoffs

After completion, ask: "Should we validate this design with prototypes or diagrams?"
