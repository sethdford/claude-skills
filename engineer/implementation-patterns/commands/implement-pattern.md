---
description: Choose and implement appropriate design pattern for the problem.
argument-hint: [problem-description or code-context]
---

Chain these steps:

1. Use the `design-patterns` skill to identify applicable patterns
2. Use the `state-management` skill if state modeling is involved
3. Use the `resource-management` skill for lifecycle management
4. Generate implementation with clear pattern application and documentation

After completion, ask: "Should we add tests to verify the pattern works correctly?"
