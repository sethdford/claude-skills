---
description: Complete REST/GraphQL API design with schema and resource modeling.
argument-hint: [feature-requirements or data-model]
---

Chain these steps:

1. Use the `rest-api-design` skill (or graphql-schema-design) to model resources/types
2. Use the `api-versioning-strategy` skill to plan evolution
3. Use the `api-error-handling` skill to define error responses
4. Use the `rate-limiting-design` skill to protect the API
5. Generate complete API schema with documentation and examples

After completion, ask: "Should we generate OpenAPI/GraphQL schema, or start testing?"
