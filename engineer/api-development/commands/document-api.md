---
description: Generate comprehensive API documentation from schema with examples and guidance.
argument-hint: [api-schema or openapi-spec]
---

Chain these steps:

1. Use the `api-documentation` skill to structure the docs
2. Use the `api-error-handling` skill to document error cases
3. Use the `rate-limiting-design` skill to include quota documentation
4. Generate interactive OpenAPI/GraphQL documentation with examples

After completion, suggest: "Share docs with clients; they can use Swagger UI or similar tools to test."
