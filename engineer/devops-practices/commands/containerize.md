---
description: Create optimized Dockerfile following best practices and security standards.
argument-hint: [application-type or existing-dockerfile]
---

Chain these steps:

1. Use the `dockerfile-best-practices` skill to design layers
2. Use the `ci-cd-pipeline-design` skill for image publishing
3. Generate Dockerfile with comments explaining each section

After completion, ask: "Should we optimize image size or add health checks?"
