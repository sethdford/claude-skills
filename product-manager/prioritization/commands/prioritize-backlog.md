---
description: Score and rank backlog items using multiple prioritization frameworks.
argument-hint: [backlog to prioritize or strategic focus]
---

Chain these steps:

1. Use the `rice-scoring` skill to quantify opportunities
2. Use the `impact-effort-matrix` skill to visualize trade-offs
3. Use the `opportunity-scoring` skill to identify whitespace
4. Use the `kano-analysis` skill to understand feature value types
5. Use the `weighted-scoring` skill to validate rankings

After completion, recommend: `build-roadmap`
